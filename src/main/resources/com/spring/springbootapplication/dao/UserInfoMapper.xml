<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="com.spring.springbootapplication.dao.UserInfoMapper">

      <!-- user新規登録 -->
      <insert id="save">
        INSERT INTO users
          (name, email, password, updated_at, created_at)
        VALUES
          (#{name}, #{email}, #{password}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
      </insert>

       <!-- user取得 -->
       <select id="findById" resultType="com.spring.springbootapplication.entity.UserInfo">
       SELECT * FROM users WHERE id = #{id}
       </select>
   </mapper>